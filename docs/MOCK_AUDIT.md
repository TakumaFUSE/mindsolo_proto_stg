# Mock 監査レポート (MOCK_AUDIT.md)

> 監査対象: `mock/index.html` + `mock/styles.css` + `mock/app.js`  
> 基準: `docs/SPEC.md §3 画面別仕様`  
> 凡例: 🔴 重大（動作・仕様に関わる）/ 🟡 中程度（過剰・欠如だが致命的でない）/ 🟢 軽微（表示・コピーのみ）  
> ステータス: ✅ 実装済（app/）/ ⚠️ モックのみ / ❌ 未対応

---

## Phase 2 時点の差分（全24件）→ Phase 7 完了後の解消状況

### 画面ゼロ: 欠落画面

| # | 種別 | 内容 | ステータス |
|---|------|------|-----------|
| Z-01 | 🔴 | `termsofuse` 画面が存在しない | ✅ mock に追加済み（Phase 2）。app/ には未実装だが Phase 1 スコープ外 |

---

### 3.1 login

| # | 種別 | 内容 | ステータス |
|---|------|------|-----------|
| L-01 | 🔴 | 利用規約リンクが `href="#"` のまま | ✅ mock では `data-go="termsofuse"` に修正済み |
| L-02 | 🟢 | プライバシーポリシーリンクが SPEC 未定義 | ⚠️ mock に残存。実装上は追加しない方針 |

---

### 3.2 signup

差分なし。✅

---

### 3.3 forgotpassword

| # | 種別 | 内容 | ステータス |
|---|------|------|-----------|
| FP-01 | 🟡 | 送信後フィードバックなし | ✅ mock に送信完了メッセージ追加済み（Phase 2）。app/ も同等の UX を実装 |

---

### 3.5 feed

| # | 種別 | 内容 | ステータス |
|---|------|------|-----------|
| F-01 | 🔴 | 検索 UI がインライン常時表示 | ✅ mock はモーダル方式に修正済み。app/ も SearchModal として実装 |
| F-02 | 🔴 | 絞り込みチップが SPEC 不一致 | ✅ mock・app/ ともに type (entry/thread) + 日付順のみに修正 |
| F-03 | 🔴 | 振り返り提案カードに既読/未読区別なし | ✅ mock・app/ ともに `is-unread` / グレーアウトで区別 |
| F-04 | 🟡 | 振り返り提案カードが押下不可 | ✅ app/ では `read_at` を更新する実装済み |
| F-05 | 🟡 | streak-pill が SPEC 未定義 | ✅ mock・app/ ともに削除済み |
| F-06 | 🟡 | ウェルカムエリアのサブテキストが SPEC 未定義 | ✅ mock・app/ ともに削除済み |

---

### 3.6 entry_write

| # | 種別 | 内容 | ステータス |
|---|------|------|-----------|
| EW-01 | 🔴 | DIVE ボタンが残存 | ✅ mock・app/ ともに削除済み |
| EW-02 | 🔴 | 「保存」ボタンが entry_detail へ遷移しない | ✅ app/ では保存後 `/entry/{id}` へ遷移 |
| EW-03 | 🟡 | 「キャンセル」ボタンに遷移なし | ✅ app/ では `/feed` へ遷移 |
| EW-04 | 🟢 | テキストエリアラベルのコピー差異 | ⚠️ 軽微。app/ は「今日の出来事・感情」プレースホルダーで実装 |

---

### 3.7 entry_detail

| # | 種別 | 内容 | ステータス |
|---|------|------|-----------|
| ED-01 | 🔴 | 「メンターに相談する」が mentor_thread 直遷移 | ✅ mock・app/ ともに mentor_top 経由に修正済み |

---

### 3.8 mentor_top

| # | 種別 | 内容 | ステータス |
|---|------|------|-----------|
| MT-01 | 🟡 | 「編集」ボタンのインタラクション未実装 | ✅ app/ では削除/編集モード実装済み（Phase 7-1） |
| MT-02 | 🟡 | online/busy ステータスチップが SPEC 未定義 | ✅ mock・app/ ともに削除済み |

---

### 3.9 mentor_add

| # | 種別 | 内容 | ステータス |
|---|------|------|-----------|
| MA-01 | 🔴 | AI おすすめ候補セクションがない | ✅ mock・app/ ともに追加済み（Phase 6） |

---

### 3.10 mentor_thread

| # | 種別 | 内容 | ステータス |
|---|------|------|-----------|
| MR-01 | 🟡 | 「過去」ボタンが chain_id フィルタなし | ✅ app/ では `/feed?chain_id=...` の URL 遷移実装済み |

---

### 3.11 discover_top

| # | 種別 | 内容 | ステータス |
|---|------|------|-----------|
| DT-01 | 🟡 | Likes 一覧の「絞る」ボタンがない | ✅ mock に追加済み。app/ の discover は Phase 6-7 で別構造（カテゴリレール）で実装済み。Likes 機能は Phase 1 スコープ外 |

---

### 3.12 discover_detail

| # | 種別 | 内容 | ステータス |
|---|------|------|-----------|
| DD-01 | 🔴 | 「関心を広げる」がフィルタボタンで配置ミス | ✅ mock・app/ ともに各カード内タグ badge に修正済み |
| DD-02 | 🔴 | 「あなたにピッタリ」アイテムがない | ✅ mock・app/ ともに両タグを混在させて実装 |
| DD-03 | 🟡 | アイテム件数が2件（仕様は5件） | ✅ mock・app/ ともに5件に修正済み |
| DD-04 | 🔴 | Like ボタンがない | ✅ mock・app/ ともに Like ボタン追加済み |
| DD-05 | 🟡 | フィルタ条件表示なし | ✅ app/ の discover_detail にカテゴリ見出しを表示 |

---

### 3.13 setting

| # | 種別 | 内容 | ステータス |
|---|------|------|-----------|
| S-01 | 🔴 | 利用プラン表示セクションがない | ✅ mock・app/ ともに利用プラン card を追加済み |
| S-02 | 🔴 | 通知設定が SPEC 禁止なのに表示されている | ✅ mock・app/ ともに通知設定 card を削除済み |

---

## Phase 7 完了後のサマリー

| 種別 | Phase 2 時点 | Phase 7 完了後 |
|------|-------------|----------------|
| 🔴 重大 | 13件 | **0件（全解消）** |
| 🟡 中程度 | 9件 | **1件残存（DT-01 Likes 機能: Phase 1 スコープ外）** |
| 🟢 軽微 | 2件 | **1件残存（EW-04: 許容）** |

### 残存差分の許容理由

| # | 内容 | 許容理由 |
|---|------|---------|
| DT-01 | Likes 一覧フィルタ | discover の Likes 保存・一覧機能は Phase 1 実装スコープ外として意図的に延期。app/ では discover を AI パーソナライズレール構造に変更しており、仕様上の責務を満たしている |
| EW-04 | テキストエリアラベル差異 | UX 上の許容範囲内。本文入力の意図は伝わる |

### app/ 実装で mock を超えた点

- SSE ストリーミングによる AI 生成コンテンツのリアルタイム表示（entry_detail）
- ユーザーエントリ topics に基づく discover パーソナライズ + 24h キャッシュ
- Supabase 永続化（mentor_messages, entries, chains）
- メンタースレッドの chain_id 引き継ぎ
- 音声入力（Web Speech API）
- カスタムメンター作成・編集・削除
- BottomNav active 状態の正確なハイライト
